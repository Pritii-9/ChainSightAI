# ============================================================
# RENDER DEPLOYMENT (free tier optimized)
# ============================================================
# Core Flask — must have
flask==3.1.3
flask-cors==6.0.2
flask-socketio==5.3.6
eventlet==0.36.1

# LLM — lightweight, no GPU
groq==0.9.0
langchain-groq==0.2.5
langchain==0.3.28

# Data & validation
pydantic==2.13.3
python-dotenv==1.2.2
requests==2.32.3
supabase==2.4.3
PyJWT==2.8.0
langchain-community==0.3.1

# Production server
gunicorn==23.0.0

# Real-time AIS
websocket-client==1.8.0
